# OpenAI GPT-4o
# Model defaults for openai/gpt-4o
#
# Merge priority: CLI > per-step config > this file > global defaults

display_name: "GPT-4o"
description: "High-performance multimodal model"
context_length: 128000

# Recommended defaults for this model
temperature: 0.7
max_tokens: 4096
top_p: 1.0
