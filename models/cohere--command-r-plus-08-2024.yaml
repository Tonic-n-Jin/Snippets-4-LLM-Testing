# Cohere Command R+ (August 2024)
# Model defaults for cohere/command-r-plus-08-2024
#
# Merge priority: CLI > per-step config > this file > global defaults

display_name: "Command R+ (08-2024)"
description: "Cohere's 104B flagship model optimized for enterprise RAG and tool use"
context_length: 128000
output_length: 4096

# Recommended defaults for this model
temperature: 0.3
max_tokens: 4096
top_p: 0.75

# Model capabilities
capabilities:
  - retrieval-augmented generation (RAG)
  - tool use
  - structured data analysis
  - multilingual (10 languages)
  - enterprise safety modes

# Model specifications
parameters_total: 104000000000

# Strengths
strengths:
  - 50% higher throughput vs previous version
  - 25% lower latency
  - Excellent RAG and citation capabilities
  - Strong tool use and API integration
  - Multiple safety modes for enterprise control

# Limitations
limitations:
  - Text-only (no multimodal)
  - Optimized for enterprise, may be overkill for simple tasks

# Prompting notes
notes: |
  Command R+ excels at enterprise RAG workflows with grounded
  generation. Use lower temperature for factual tasks. Strong
  in 10 languages including Japanese, Korean, and Arabic.
  Enable appropriate safety mode for your use case.
